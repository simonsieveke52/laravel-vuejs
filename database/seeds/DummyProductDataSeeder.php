<?php

use Illuminate\Database\Seeder;

class DummyProductDataSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        //set flags to turn off remote
        //Truncate the needed tables
        //Create product data
        //Create product relation data
        //Create category data
        //Create category to product relation data
    }
}
